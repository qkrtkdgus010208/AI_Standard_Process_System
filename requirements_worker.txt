# ==============================================================================
# AI Standard Process System - 작업자 단말 의존성 (requirements_worker.txt)
# ==============================================================================
# 현장 작업자 단말 프로그램 (Jetson Orin Nano / Linux PC) 전용 의존성 목록입니다.
# [실행 방법]
#   Jetson/Linux 실행 스크립트: bash run_worker.sh
#
# [수동 설치 시 (프로젝트 최상위 루트 디렉토리 기준)]
#   pip install -r requirements_worker.txt
# ==============================================================================

# ------------------------------------------------------------------------------
# [하드웨어 카메라 제어 도구 (Linux 시스템 패키지)]
# ------------------------------------------------------------------------------
# v4l2-ctl (설치: sudo apt install -y v4l-utils)
# ※ 주의: v4l2-ctl은 Python pip 패키지가 아닌 Linux 시스템 CLI 유틸리티(v4l-utils)입니다.
#   pip install 시 배포본 없음 오류가 발생하므로 시스템 패키지로 설치해야 합니다.
# ------------------------------------------------------------------------------

# [1] GUI 프레임워크 (Worker 작업자 단말 GUI)
# ARM64/Jetson 환경에서는 PyPI 바이너리 대신 시스템 패키지(sudo apt install python3-pyqt5) 및
# venv(--system-site-packages)를 연동하여 사용할 수 있습니다.
PyQt5>=5.14.0,<6.0.0

# [2] 비전 및 영상 처리 (USB/CSI 카메라 영상 캡처 및 전처리)
opencv-python-headless>=4.8.0
numpy>=1.23.0,<2.0.0

# [3] AI 비전 판정 및 모델 추론 (YOLOv8 조립 정합성 검사)
ultralytics>=8.0.0

# [4] 하드웨어 통신 (STM32F411RE UART 시리얼 제어)
pyserial>=3.5

# [5] 유틸리티 및 타입 호환성
typing_extensions>=4.5.0

# [6] 개발 및 테스트 도구 (단위 테스트, 정적 분석)
pytest>=7.0.0
pytest-qt>=4.2.0
pytest-cov>=4.0.0
flake8>=6.0.0
black>=23.0.0
